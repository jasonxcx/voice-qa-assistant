事项清单：
- 配置中的 provider 会被代码中的默认值覆盖
- STT 模型未下载时应该弹窗，显示下载进度和下载速度

待AI解决：
- 增加快捷键操作，监听全局的键盘输入
- 加载完STT模型以后，应该还可以修改LLM模型配置
- lm studio模型调用报503
- 在字幕窗口增加手动/自动转录的切换
- 加载完模型后立马就开始录音了，应该加载完模型后等点击“开始监听”按钮后才开始录音

已人工验证：
1. 是否进行转录不再由系统根据语音大小、时间间隔自动判断 
2. 字幕窗口增加按钮，可以“开始监听”和“停止监听“ 
3. 字幕窗口样式调整：所有按钮都并排放置，翻页按钮放在右下角，”隐藏“按钮颜色调成灰色
4. 主窗口大模型设置希望是使用Ollama或者OpenAI时手动填写模型名称，使用LM Studio时通过接口查询到有哪些模型并可以下拉选择
5. 主窗口模型模式为LM Studio时，不应该显示“模型名称”
6. 主窗口模型模式为LM Studio时会刷新可选模型列表，但是如果是用户之前配置了一个模型，列表刷新后应该先去匹配是否存在，而不是默认选择第一个；即使匹配不上也应该优先用loaded_instances不为空的模型；loaded_instances不为空的模型在下拉选择时应该排在前面，且颜色做区分
7. 开始监听后，只有当点击停止监听后，才将一整段语音拿去转录
8. 回答字幕刷新
   - 大模型生成回答需要将问题的回答流式地显示在字幕中，即大模型每吐一个字，字幕就跳一个字。
   - 现在print已经实现了，但是字幕会卡在”正在生成回答...“，一直不出结果；即使api已经返回了完整的结果，也不会更新字幕
   - 预期是每个问题占一页，问题在第一行，回答放在本页的后面；当问下一个问题时切换到下一页
9. 字幕窗口会在点击"开始监听"按钮，模型加载完成后显示